<UserControl x:Class="UIWPF.Views.NodeGroupEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:local="clr-namespace:UIWPF.Views"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:UIWPF.ViewModels"
             d:DataContext="{d:DesignInstance vm:NodeGroupEditViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">

        <Grid Margin="10,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical"  Width="205">
            <Label  Content="Group name" Foreground="White"/>
            <TextBox HorizontalAlignment="Left" Text="{Binding GroupName}" Grid.Row="1" Width="205" />
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,40,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Content="Save" Command="{Binding CloseDialogCommand}" CommandParameter="true" IsDefault="False" Width="100"></Button>
            <Button Margin="5,0,0,0" Command="{Binding CloseDialogCommand}" CommandParameter="false" IsDefault="True" Content="Cancel" Width="100"></Button>
        </StackPanel>
    </Grid>
       

</UserControl>
